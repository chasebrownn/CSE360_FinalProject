import javax.swing.*;
import javax.swing.table.DefaultTableModel;
import java.awt.*;
import java.util.List;

/*
public class AttendanceTable extends JFrame {

    JTable frame = new JTable();
    DefaultTableModel model = new DefaultTableModel();
    JScrollPane scroll;
    String headers[] = {"ASURITE", "Time"};

    public AttendanceTable(List<Attendance> attendance){
        model.setColumnIdentifiers(headers);
        frame.setModel(model);
        scroll = new JScrollPane(frame);

        for (int i = 0; i < attendance.size(); i++) {
            model.addRow(new Object[]
                    {
                            attendance.get(i).getAsurite(),
                            attendance.get(i).getTime() });
                    }
    }

}*/


